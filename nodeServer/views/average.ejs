<html>
    <head>
        <link rel="stylesheet" href="website/style.css">
        <title>Flow Finder</title>
        <meta charset="utf-8">
        <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.7.2/Chart.min.js"></script>
    </head>
    <% include style %>
    <body>
    	<%include navbar %>
	<br>
	<br>
	<div id="chart">
            <canvas id="myChart"></canvas>
        </div>
	<script>
	    var names = [];
	    var avgLitres = [];
	    <% for (var i = 0; i < data.length; i++){%>
		names.push("<%= data[i].name %>");
		


		//avgLitres.push(<%= data[i].avgLitres/3.85 %>);

		
		//Trent Test
		avgLitres.push(<%= Math.round((data[i].avgLitres/3.85)*10)/10%>);




            <%}%>
            var ctx = document.getElementById("myChart").getContext('2d');
	    Chart.defaults.global.defaultFontSize = 20;
	    Chart.defaults.global.defaultFontColor = 'black';
            var myChart = new Chart(ctx, {
                type: 'bar',
                responsize: true,
                data: {
                    labels: names,
                    datasets: [{
                        label: 'Average Water Consumption (Gallons)',
                        data: avgLitres,
                        
                        backgroundColor: [
                            'rgba(255, 0, 0, 1)',
                            'rgba(0, 255, 0, 1)',
                            'rgba(0, 0, 255, 1)',
                            'rgba(255, 255, 0, 1)',
                            'rgba(0, 255, 255, 1)',
                            'rgba(255, 0, 255, 1)'
                        ],
                        borderColor: [
                            'rgba(255,99,132,1)',
                            'rgba(54, 162, 235, 1)',
                            'rgba(255, 206, 86, 1)',
                            'rgba(75, 192, 192, 1)',
                            'rgba(153, 102, 255, 1)',
                            'rgba(255, 159, 64, 1)'
                        ],
                        
                        borderWidth: 1
                    }]
                },
                options: {
                    scales: {
                        yAxes: [{
                            ticks: {
                                beginAtZero:true
                            }
                        }]
                    }
                }
            });
 
        </script>    
    
    </body>
</html>
